<%= form_with model: player, url: game_players_path(player.game) do |f| %>
  <%= f.text_field :name %>
  <% player.errors[:name].each do |message| %>
    <%= message %>
  <% end %>
  <br>
  <%= f.submit 'Continue' %>
<% end %>
